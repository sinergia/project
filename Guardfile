# 1) Run at app root folder: guard start
# 2) Enable livereload Chrome Extension
# https://github.com/guard/guard#readme

guard 'livereload' do
  watch(%r{php/tpl/.+\.php$})
  watch(%r{assets/.+\.(css|js)})
end
